// Global test setup
// Prisma client mock is handled via resolve alias in vitest.config.ts

// Extend Vitest matchers with jest-dom (toBeInTheDocument, toHaveTextContent, etc.)
import "@testing-library/jest-dom/vitest";
