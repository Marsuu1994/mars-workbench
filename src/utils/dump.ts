/**
 * Dump feature constants — client-safe; shared by the zod schema, the server
 * action's paging, and the composer/feed UI (textarea maxLength, skeletons).
 */
export const DUMP_PAGE_SIZE = 20;
export const DUMP_ENTRY_MAX_LENGTH = 10000;
