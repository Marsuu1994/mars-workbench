/** Route to the plan-creation page (shared by both empty-board variants). */
export const CREATE_PLAN_HREF = '/kanban/plans/new';

// User-facing empty-board copy now lives in i18n (`Board.Empty`) — render it
// via getTranslations in the EmptyBoard server component.
